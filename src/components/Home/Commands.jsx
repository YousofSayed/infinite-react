import React from 'react'
import { AsideControllers } from './Protos/AsideControllers'
import { cmds } from '../../constants/cmds';

export const Commands = () => {

  return (
    <section>
        <AsideControllers/>
    </section>
  )
}
